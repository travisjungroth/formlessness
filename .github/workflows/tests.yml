name: Tests

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  unit_tests:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        python-version: ['3.9', '3.10']

    steps:
    - uses: actions/checkout@v2
    - name: Set up Python ${{ matrix.python-version }}
      uses: actions/setup-python@v3
      with:
        python-version: ${{ matrix.python-version }}
        cache: poetry
    - name: Load cached local for Poetry
      id: cached-poetry-install
      uses: actions/cache@v3
      with:
        path: ~/.local
        key: local-${{ runner.os }}-${{ hashFiles('.github/workflows/tests.yml') }}-${{ matrix.python-version }}
    - name: Install Poetry
      if: steps.cached-poetry-install.outputs.cache-hit != 'true'
      uses: snok/install-poetry@v1
    - run: poetry install --no-interaction
    - run: poetry run doit test

  format:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2
      with:
        ref: ${{ github.head_ref }}
    - name: Set up Python
      uses: actions/setup-python@v3
      with:
        cache: poetry
    - name: Load cached local for Poetry
      id: cached-poetry-install
      uses: actions/cache@v3
      with:
        path: ~/.local
        key: local-${{ runner.os }}-${{ hashFiles('.github/workflows/tests.yml') }}
    - name: Install Poetry
      if: steps.cached-poetry-install.outputs.cache-hit != 'true'
      uses: snok/install-poetry@v1
    - run: poetry install --no-interaction
    - run: poetry run doit format_all
    - uses: stefanzweifel/git-auto-commit-action@v4
    - name: Fail if changes
      if: steps.auto-commit-action.outputs.changes_detected == 'true'
      run: exit 1
